# Tests expect a local PostgreSQL instance.
# Create this database first (example: `createdb fundpath_test`) or replace
# DATABASE_URL with a connection string for an existing test database.
DATABASE_URL=postgresql://postgres@localhost:5432/fundpath_test
JWT_SECRET=test-only-secret-not-for-prod
